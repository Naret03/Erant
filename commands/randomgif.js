const randomGifAPI = "https://g.tenor.com/v1/random?key=LIVDSRZULELA" //&q=excited
const https = require("https");

module.exports = {
    name: ["randomgif", "rgif", "rg"], 
    description: "Random gif from Tenor website",
    execute(msg, args){
    }
}